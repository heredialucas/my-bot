import { DashboardProps } from "../types";

export default function Dashboard(props: DashboardProps) {
    return (
        <div className="space-y-6">
            <p>Dashboard content will be implemented in future versions.</p>
        </div>
    )
}